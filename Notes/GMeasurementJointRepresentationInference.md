# A conjunction of separate representation certificates

This object states that every required subtotal in the two publications has at
least one admissible hidden-precision realization under its own declared model.
It joins three existing existential statements: HUST 2018 AAF combined,
Schlamminger 2006 statistical, and Schlamminger 2006 systematic. No latent
variable is shared across publications. No search or historical recalculation
is performed.

The measured G values are neither averaged nor compared. No probability that
rounding occurred follows, and no common cause, common systematic, statistical
agreement, or universality of G is inferred. The name
`all_representable_under_separate_declared_models` records this narrow meaning.
These known input outcomes are prior knowledge; the freeze is prospective only
about the new inference semantics, not a blind prediction of those outcomes.

The established certificates record the first successful candidate in a schedule
whose first entry is the unshifted midpoint. HUST and Schlamminger's statistical
scope use a later, shifted witness. Schlamminger's systematic scope already works
at the midpoint. The projection preserves that difference by candidate identity;
it does not reproduce candidate measurements or introduce scores, weights,
evidence counts, or probabilities. This narrowly incorporates DM-050.

An incompatible scope makes its publication not representable; one such
publication makes the joint proposition not all representable. If all required
scopes are compatible, their conjunction is representable. All remaining cases
are undetermined. In particular, unresolved from the historical primitive is
schedule-relative: the diagonal schedule does not exhaust the admissible box.
An unresolved input therefore cannot establish nonexistence. FH-2 is handled by
this semantic limit; a richer search belongs in the next new measurement's
prospective preregistration, not in altered historical certificates.

Only the PR #39 and PR #40 result files are scientific inputs. The calculator
opens them through a literal allowlist, checks their frozen hashes, and never
follows their embedded source references. The disputed E-001 authorization and
source-audit artifacts, the combined MeasurementModel and evidence classifications
are outside the dependency path. Source/history metadata protects provenance and
does not supply scientific operands. Preservation reads are separate integrity
checks and exclude all disputed E-001 files.

The shared strict freeze verifier enforces the preregistration-only first commit,
sole-parent base, unchanged bytes and preserved ancestry. GitHub's draft PR creation
at 2026-09-08T01:50:02Z externally anchors the freeze; implementation commit clocks
provide corroborating order, not an independent timestamp authority. Snapshot and
mutation attestations bind committed source and tests. CI guards need no external
scientific downloads. Merge with **Create a merge commit** only.
